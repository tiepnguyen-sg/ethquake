# Beacon API fixtures

These minimized responses were recorded from the Phase 1 Lighthouse node on
2026-08-17. Unused fields were removed only from the runtime-spec response;
the head and finality responses are preserved as received.
